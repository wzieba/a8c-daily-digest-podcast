#!/usr/bin/expect -f

set timeout 600
log_user 1

# Start claude with --dangerously-skip-permissions to bypass all prompts
spawn claude --dangerously-skip-permissions /context-a8c:digest

# Wait for process to complete
expect eof

wait
